# RPI Global Context — RETIRED

> **This file is no longer the source of truth.** Retired 2026-06-16 per JDM directive.
>
> The Machine's operating context now lives in **The Scroll** — the live warrior surface
> generated by `scroll-updater` and read at boot via `launch-warrior.sh`. Warriors boot off
> their per-warrior doc tree (`toMachina/docs/warriors/<warrior>/`) + the inlined shared
> Scroll streams (identity, operating rules, terminology, mission/doctrine). No warrior
> needs this file.
>
> **For scratch / non-warrior sessions on this box:** project context lives in each repo's
> own `CLAUDE.md` (e.g. `toMachina/CLAUDE.md`). Platform/governance doctrine lives in
> `_RPI_STANDARDS/reference/os/` and the Scroll shared streams.
>
> The full pre-retirement content is in git history (`git log -p -- CLAUDE.md`).
> The auto-generators that used to rebuild this file (knowledge-promote, claude-md-writer,
> claude-md-tracker) were decommissioned the same day — they no longer exist on this box.

🥷 — SHINOB1, CTO — retired in favor of The Scroll
